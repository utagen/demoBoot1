server:
  port: 8085

mybatis:
  type-aliases-package: com.sisp.dao.entity
  mapper-locations: classpath:mapper/*.xml
  # 开启Mybatis的二级缓存
  configuration:
    cache-enabled: true

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/myweb?serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    username: root
    password: abc123
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    port: 6379
    host: 127.0.0.1
    timeout: 10000ms
    jedis:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0



